
#include <slide-merger-task.hpp>


void SlideMergerTask::operator()(const cv::Mat & inputFrame, cv::Mat & outputFrame) {
    
}

void SlideMergerTask::loadImages(std::vector<cv::Mat> & result) {
    for (int i = _startIndex ; ; ++i) {
        
    }
}
